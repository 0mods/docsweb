---
sidebar_position: 1

title: ⚙️ Конфиг НИПа
id: npc-configure
description: Здесь написано и изменяемых настройках НИПа
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import styles from '../../.resources/styles.module.css';

:::info Всё перечисленное здесь, пишется
```kts
<npcID>.configure {
  // т.е. здесь
}
```
:::

## Как работает

:::note Идём в "Спавним НИПа"
>  Поддерживаются почти все настройки, которые используются при [Создании НИПа](./0-npc-create.mdx)
:::

## Дополнительные настройки

:::note Дополнительные настройки
>  Все что написано в этой части, не относится в данному разделу, но подходит по смыслу.  
>  Все написанное здесь, не пишется в `<npcID>.configure{...}`.
> ```kts
> // Изменить имя НИПу
> 
> // Колизия хитбокса
> <npcID>.hitboxMode = HitboxMode.<Type>
> /* 
>  * Есть:
>  * - BLOCKING | Вы не сможете сдвинуть нпс (будет стоять как камень)
>  * - PULLING  | Классический режим, когда подойдя к сущности вы отталкиваетесь друг от друга
>  * - EMPTY    | Без коллизии. Проходить сквозь сущности без преград
> */
> 
> <npcID>.invulnerable = <false/true> // Игнорирование урона НИПа
>
> <npcID>.name = "<newName>" // Новое имя НИПа
> ```
:::

---

## Примеры

<Tabs>
<TabItem value="configureNPC" label="Пример настроек" attributes={{className: styles.def}}>

```kts
jeka.configure{
    //name - эту настройку нельзя изменить в данном месте
    model = "my-mod:models/entity/personal.glb"
    textures["gltf_model_0_layer_gltf"] = "my-mod:textures/entity/creatures.png"
    
}
```

</TabItem>
<TabItem value="hitboxMode" label="Режим хитбокса" attributes={{className: styles.def}}>

```kts
jeka.hitboxMode = HitboxMode.BLOCKING
```

</TabItem>
<TabItem value="ignoreDamage" label="Игнорировать урон" attributes={{className: styles.def}}>

```kts
jeka.invulnerable = true
```

</TabItem>
<TabItem value="changeName" label="Изменить имя НИПу" attributes={{className: styles.def}}>
```kts
jeka.name = "Пашка"
```

</TabItem>
</Tabs>