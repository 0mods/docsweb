---
sidebar_position: 11

title: 💅 Текстура на НИПа
id: npc-texture
description: Теперь есть кожа на НИПе
---

import ToBeta from '@site/src/components/NewDocs.js';

<ToBeta url='welcome' />

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import styles from '@site/src/css/styles.module.css';

:::info Текстура на НИПа

<Tabs>
  <TabItem value="set_texture" label="Установить текстуру" attributes={{className: styles.def}}>
    
    ```kts
      textures["<layer>"] = "<pack/to/texture>" // Установить на слой layer текстуру - texture
    ```
    > Устанавливает определённую текстуру на определённый слой на моделе
    
  </TabItem>
  <TabItem value="get_texture" label="Как получить <layer>?" attributes={{className: styles.def}}>
    > Чтобы получить `<layer>`, нужно прописать команду `/hollowengine model <path/to/model>`, Вам в чат покажет список всех анимаций и в самом низу будет список всех существующих слоём текстур, которые есть на модели.  
    > Теперь просто кликните по нужной сточке и у вас скопируется название, после вставьте это название в `["<layer>"]` в кавычки.
  </TabItem>
</Tabs>

:::