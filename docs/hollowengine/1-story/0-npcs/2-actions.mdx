---
sidebar_position: 2

title: Actions
id: npc-actions
desc: 'NaN'
---

import '@site/src/css/pages/pc-phone-ver.css';

<div align="center">

# NPC actions

---

<br />

## Walk

<div align="left" id="dont-copy">

<div id="ver-full">

<div style={{ "display": "flex" }}>
  <div style={{ "align-items": "flex-start", "width": "100%", "margin-right": "5px" }}>
  
  ```kts title="Function to positon"
  fun NPCEntity.move(
    pos: Vec3,
    dist: Double = 1.5,
    speed: Double = 1.0
  )
  ```
  
  </div>
  <div style={{ "align-items": "flex-end", "width": "100%", "margin-left": "5px" }}>
  
  ```kts title="Function to entity"
  fun NPCEntity.move(
    entity: Entity,
    dist: Double = 1.5,
    speed: Double = 1.0
  )
  ```
  
  </div>
</div>
</div>

<div id="ver-cut">

```kts title="Function to positon"
fun NPCEntity.move(
  pos: Vec3,
  dist: Double = 1.5,
  speed: Double = 1.0
)
```
```kts title="Function to entity"
fun NPCEntity.move(
  entity: Entity,
  dist: Double = 1.5,
  speed: Double = 1.0
)
```

</div>

```kts title="Examples"
// NPC will moves to position
npcID move pos(92, 23, 56)

// NPC will follow player
val player = server.playerList.players.first() // Получаем первого игрока из списка
npcID move player

// The NPC will move to the point until the distance is less than 3.0 blocks, with a speed of 2.0
npcID.move(pos(25, 64, 2), 3.0, 2.0)

// The NPC will move towards the player until the distance is less than 3.0 blocks, at a speed of 2.0
npcID.move(player, 3.0, 2.0)
```

</div>

<br />
---
<br />

## Look

<div align="left" id="dont-copy">

<div id="ver-full">

<div style={{ "display": "flex" }}>
  <div style={{ "align-items": "flex-start", "width": "100%", "margin-right": "5px" }}>
  
  ```kts title="Function to positon"
  fun NPCEntity.look(pos: Vec3)
  ```
  
  </div>
  <div style={{ "align-items": "flex-end", "width": "100%", "margin-left": "5px" }}>
  
  ```kts title="Function to entity"
  fun NPCEntity.look(entity: Entity)
  ```
  
  </div>
</div>
</div>

<div id="ver-cut">

```kts title="Function to positon"
fun NPCEntity.look(pos: Vec3)
```
```kts title="Function to entity"
fun NPCEntity.look(entity: Entity)
```

</div>

```kts title="Example"
// НИП посмотрит в точку
npcID look pos(92, 3, 24)

// NPC looks to player
val player = server.playerList.player.first() // Получаем первого игрока из списка
npcID look player
```

</div>

<br />
---
<br />

## Interaction with the world

### Interaction with the player

<div align="left" id="dont-copy">

```kts title="Function"
fun NPCEntity.useBlock(pos: Vec3)
```
```kts title="Example"
// NPC simulates right-clicking on a block
npcID useBlock pos(92, 93, 241)
```

</div>

<br />

## Destroy block

<div align="left" id="dont-copy">

```kts title="Function"
fun NPCEntity.destroyBlock(pos: Vec3)
```
```kts title="Example"
// NPC destroy block
npcID destroyBlock pos(92, 46, 24)
```

</div>

<br />
---
<br />

## Drop item

<div align="left" id="dont-copy">

```kts title="Function"
fun NPCEntity.dropItem(item: ItemStack)
```
```kts title="Example"
// NPC will drop item "Golden Apple'
npcID dropItem item("golden_apple")
```

</div>

<br />
---
<br />

## Say

<div align="left" id="dont-copy">

```kts title="Function"
fun NPCEntity.say(text: String)
```
```kts title="Example"
npcID say "Hello!"
```

</div>

</div>