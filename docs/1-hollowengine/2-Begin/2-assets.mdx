---
sidebar_position: 2
---

# ↳ ▶ Assets 📁

:::info assets
> Зачастую при использовании мода тебе могут потребоваться свои ресурсы, или замена ресурсов других модов: текстуры, модели, звуки и т.п.
> Для начала создай новую папку в директории мода (`hollowengine`) с названием `assets`, и в ней же создай новую с любым названием (допускаются только английские буквы **нижнего регистра**, цифры, нижнее подчёркивание и символ `/`).
> ```
> hollowengine
> \- assets
>    \- <📁 modid> <- например: hollowengine, my_mod, custom, jogol и т.д.
> ```
> ▷ В ней ты уже можешь размещать ресурсы, как в ресурспаках.

---

## Модели

> Хранить ресурсы вы можете где и как хотите. Но рекомендуем хранить в этих папках:
> - `hollowengine/assets/<modid>/models/entity/*` - Модели,
> - `hollowengine/assets/<modid>/textures/entity/*` - Текстуры к моделям.

> Поддерживаемые форматы моделей:  
> - `GlTF` - `.gltf`. Для некоторых моделей может потребовать файла `<name>.bin` рядом с `<name>.gltf`.  
> - `Glb` - `.glb`.

---

## Sounds resources

> Звуки хранить строго в `hollowengine/sounds/*`. Иначе вам придётся для каждого звука самому создавать [sounds.json](https://minecraft.fandom.com/ru/wiki/Sounds.json), а при наличии озвучки прописывать там более 100 файлов будет занимать очень много времени.

---

## Другие ресурсы

> Все остальные ресурсы можете хранить как угодно. Но, опять же, помните что путь к модели указывается только английскими буквами **нижнего регистра** и цифрами. Если не понятно, то рекомендую скачать первый попавшийся ресурспак, открыть как архив и изучить как он внутри устроен, иначе очень высока вероятность, что ты в последствии запутаешься при добавлении своих моделей или текстур.

---
:::

## ResourceLocation (Путь к ресурсу в скрипте)

Чтобы указать путь к ресурсу в скрипте вам необходимо указать путь относительно вашей папки `modid`. Вот несколько примеров как можно указать путь:

| ResourceLocation (в скрипте) | Путь к ресурсу (в проводнике) |
| --- | --- |
| `"botania:models/entity/my_botanic_npc.gltf"` | `.minecraft/hollowengine/assets/botania/models/entity/my_botanic_npc.gltf` |
| `"story_mod:vitalik_model.gltf"` | `.minecraft/hollowengine/assets/story_mod/vitalik_model.gltf` |
| `"hollowengine:models/player_entity.gltf"` | `.minecraft/hollowengine/assets/hollowengine/models/player_entity.gltf` |