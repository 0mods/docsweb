---
sidebar_position: 0
---

# ↳ ▶ Кастомный интерфейс 📺 `[WORK IN PROGRESS...]`

:::caution "Информация ещё не полная, но то что тут уже - рабочая."  
![Ожидание...](../../.resourses/he-loading.gif)  
:::

:::info  Кастомные интерфейсы  
```kts
gui {
    // Всё что рассписано ниже, пижется сюда
}
```
:::

---

:::note Элементы
```kts
    image("<path/to/texture>.png>") {
        ... // Параметр элемента
    }
    
    // Кнопка
    button("<text>", "<path/to/texture>.png") {
        ... // Параметры элемента
        
        // Действие
        onClick {
            ... // Выполнение действия, когда данная кнопка была нажата

            close() // После нажатия кнопки, GUI (Интерфейс) закроется
        }
    }
    
    // Текст
    label("<text>")
    
    // Сущность
    entity( <Entity: entity/player>, <npcID> ) {
        ... // Параметры элемента
    }
```
:::

## Параметры элемента

:::note могут указываться внутри элементов для их настройки
```kts
// Размер элемента
size( <x>.<PIX>, <y>.<PIX> ) // Если указан в начале в gui{...}, то задаётся общий размер GUI (интерфейса)

// Смещение элемента
offset( <x>.<PIX>, <y>.<PIX> )

// Смещение от краёв
padding(
  top = <num>.<PIX>,
  left = <num>.PIX>,
  botton = <num>.<PIX>,
  left = <num>.<PIX>
)

// Выравнивание относительно элемента
aling( Aligment.<TYPE> )
/*
 * TYPE:
 * - TOP_LEFT      | Сверху влево
 * - TOP_CENTER    | Сверху в центре
 * - TOP_RIGHT     | Сверху вправо
 * - ---           | ---
 * - LEFT_CENTER   | В центре слева
 * - CENTER        | В центру
 * - RIGHT_CENTER  | В центре справа
 * - ---           | ---
 * - BOTTON_LEFT   | Снизу слева
 * - BOTTON_CENTER | Снизу в центре
 * - BOTTON_RIGHT  | Снизу справа
*/

// Слой
zLayer = <num>
```
:::



> `PIX`:
> - `px` - В пикселях
> - `pw` - В % от пред. виджета
> - `ps` - В % от окна игры

---

Примеры:
```kts

```