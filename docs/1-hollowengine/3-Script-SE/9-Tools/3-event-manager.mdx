---
sidebar_position: 3
---

# ↳ Менеджер ивентов 🕴️

:::info Интерфейс открывается...
> Нажатием кнопки `~` или `Ё`.
:::

---

:::note Менеджер ивентов
```kts
// Добавить в список
ProgressManager.addMessage { "<text>" }

// Убрать из списка
ProgressManager.removeMessage { "<text>" }

// Очистить список
ProgressManager.clear()
```
:::

---

## Примеры
:::info -
```kts
ProgressManager.addMessage{"В добрый путь, молодец!"} // Новый ивент в список
...
ProgressManager.removeMessage{"В добрый путь, молодец!"} // Удалить из списка ивентов по тексту
// Для удаления существующего ивента, нужно указать тот же текст
// или же можно сделать так
val mess by ProgressMessage{"Я в дорогу, дорогая"} // Новый ивент в список и запись в переменную
...
ProgressMessage{mess} // Удалить из списка ивентов - привязанный текст к переменной mess
```
:::