---
sidebar_position: 3
---

# Анимации NPC

Хорошо когда NPC как - живой.

## Стандартные анимации

> К стандартным относяться:

- Idle - Когда стоит на месте без движения,

- Walk - Когда ходит. Стандартное передвижение,

- Death - Когда умирает,

- Sit - Когда сидит(на коне, свинье и т.д.).

---

## Дополнительные анимации

```jsx
<npcID>.play("[<animName>]", <priority>, [<playType>])
```

> Инфо:

- `<animName>` - Имя анимации в моделе. | Обязателен,

- `<priority>` - Какой дать приоритеть для анимации в отличии от остальных. Нуждается если анимация воспроизводиться криво. `[ priority = <number>f ]`,

- `<playType>` - Определяет Как воспроизводить анимацию. Какие типы есть -> [Типы воспроизведения анимаций](../extra/playType).

Вот что вышло у меня:

```jsx
vit.play("addAnim.waving", playType = PlayType.ONCE)
```

![Виталик машет вам](../../../static/img/he-guide/animWaving.gif)

---

Далее: [NPC разговаривает](./talk-npc)