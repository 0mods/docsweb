---
sidebar_position: 9
title: Шаблоны строк
id: mkd-string-templates
description: 'Использование строковых шаблонов для удобного форматирования'
---

{/* Спасибо 'dan_bat' за данную страничку :) */}

<div align="center">

# Шаблоны строк в `Kotlin`

---

<br />

:::tip Что это?
Строковые шаблоны позволяют встраивать переменные и выражения прямо в строки с помощью символа `$`. Это упрощает форматирование и делает код чище.
:::

<br />

---

<br />

## Базовое использование

<div align="left">

### Вставка переменных

```kts title="Пример"
val name = "Анна"
println("Привет, $name!") // Привет, Анна!
```

### Вставка выражений

```kts title="Пример с выражением"
val a = 5
val b = 3
println("Сумма: ${a + b}") // Сумма: 8
```

### Доступ к свойствам

```kts title="Пример с объектами"
data class User(val login: String)
val user = User("admin")
println("Логин: ${user.login}") // Логин: admin
```

</div>

<br />

---

<br />

## Многострочные строки

<div align="left">

### Тройные кавычки

```kts title="Пример"
val text = """
  Строка 1
  Строка 2
  Строка 3
""".trimIndent()

println(text)
```

```console title="Вывод"
Строка 1
Строка 2
Строка 3
```

:::tip `trimIndent()`
Удаляет начальные пробелы для красивого форматирования.
Работает так же и без *данного* метода :/
:::

</div>

<br />

---

<br />

## Экранирование символов

<div align="left">

### Спецсимволы

```kts title="Пример экранирования"
println("Символ \$ не сработает") // Символ $ не сработает
println("Первая строка\nВторая строка")
```

### RAW-строки

```kts title="Пример без экранирования"
val regex = """\d+\.\d+""".toRegex() // Регулярка для чисел с точкой
```

:::warning Уточнение
Представленный пример в буквальном смысле создает выражение, если не очень понятно обратитесь к ИИ. Просто в примере просто объяснить очень сложно...
:::

</div>

<br />

---

<br />

## Продвинутые сценарии

<div align="left">

### Логика внутри шаблонов

```kts title="Условные выражения"
val score = 85
println("Результат: ${if (score > 70) "✅ Успех" else "❌ Провал"}")
```

### Форматирование чисел

```kts title="Пример с форматированием"
val price = 1234.5678
println("Цена: ${"%.2f".format(price)} ₽") // Цена: 1234.57 ₽
```

</div>

<br />

---

<br />

:::warning Ограничения

- Нельзя использовать `$` для полей **nullable-типов** без проверки на null:  
  `println("Значение: ${value?.length}")`
- Избегайте сложной логики внутри `${}` для читаемости кода.
:::

</div>
