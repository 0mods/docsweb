---
sidebar_position: 1

title: Переменные
id: mkd-val-and-var
description: 'Поговорим о том - что такое переменные и с чем их едят за завтрак, обед и ужин'
---

<div align="center">

# Переменные в `Kotlin`

---

<br />

:::tip Что такое переменная?
**Переменная** — это именованное место в памяти, которое используется для хранения данных,
которые огут изменяться в процессе выполнения программы. Переменные позволяют программам
сохранять, изменять и получать значения данных.

```kts title="Примеры"
val i = 10 // В этой переменной будет харанится число 10
val k = 2 // А в этой переменной будет хранится другое число, число 2
val s = "Hellow World" // Такая переменная уже хранит не число, а строку (или же "Текст")
val d = 5.24 // Такая переменная буде тхранить уже не целое значение (десятичная)
```

Вы можете обозвать переменную как в душе угодно.
:::

<br />

---

<br />

## Типы переменных

Есть только 2 типа переменных и они отличаются друг от друга.

<div align="left">
- Тип `Value`. Такой тип переменных является статичным и не может никак менять своё значение
после.
  - ```kts
    val // От слова value - значение
    ```
  - ```kts title="Примеры"
    /*
     * Создаём статичный тип переменной,
     * обозваем его number
     * и записываем в неё число 10
    */
    val number = 10

    /*
     * Создаём статичный тип переменной,
     * обзываем его name
     * и записываем в него текст "Pavel"
    */
    val name = "Pavel"
    ```
- Тип `Variable`. Такой тип переменных является динамическим и может изменить своё значение
  после в любой момент.
  - ```kts
    var // От слова variable - изменяемый
    ```
  - ```kts title="Примеры"
    /*
     * Создаём перезаписываемую переменную,
     * обзываем её как number,
     * устанавливаем значение по умолчанию 5
     * и после меняет значение этой переменной на 15
    */
    var number = 5
    number = 15

    /*
     * Создаём перезаписываемую переменную,
     * обзываем её как lastName,
     * устанавливаем значение по умолчанию "Kyersher"
    */
    var lastName = "Kyersher"
    lastName = "Belash"
    ```
  - <span>
    :::warning Обратите внимание
    Изменить можно на этот же тип данных, но никак не на другой, т.е.:
    ```kts
    var number = 10

    number = "Pavel" // Такое нельзя делать, это грубая ошибка.
    /* К вам прийдёт дядя Холоу и въебёт вам по затылку за такое 🙂 */
    ```
    ---
    Но, если вы изначально укажите, какой тип данных принимает в себя переменная как `Any?` -
    то тогда сможете спокойно менять значние на что угодно 🙂.
    ```kts
    var rValue: Any? = "Vinigret"
    rValue = 10.256
    ```
    :::
    </span>
</div>

<br />

---

<br />

## Уточнение

Так же вы можете указать **какой тип данных** сможет принимать в себя та или иная переменная,
просто поставив после имени `:` и указав нужынй класс:
```kts
val onlyIntValue: Int = 10 // Принимает только целые числа
val onlyDoubleValue: Double = 0.2563 // Принимает только десятичные числа
val onlyStringValue: String = "Only String" // Принимает только строку/текст
```

</div>