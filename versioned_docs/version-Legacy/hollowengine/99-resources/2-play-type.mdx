---
sidebar_position: 2

title: 'Класс: PlayType'
id: play-type
desc: 'Типы анимации в виде класса'
---

<div align="center">

# PlayType

</div>

```kts
enum class PlayType {
  ONCE, // Воспроизводит 1 раз
  LOOPED, // Воспроизводит циклично (без остановки)
  FREEZE, // Заморозка на последнем кадре
  REVERSED // Воспроизводит циклично (без остановки) туда-сюда
}
```